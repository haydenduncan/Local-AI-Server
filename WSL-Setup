This is how to setup WSL (Windows Subsystem for Linux) on Windows

1. Open terminal in Windows
2. Runs wsl --install
3. Reboot your PC
4. Enter a username and password

Update and Upgrade Linux
1. sudo apt update
2. sudo apt upgrade -y
